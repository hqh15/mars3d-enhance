import { initMap, removeLayerById, removeLayersByIds } from './map'
import { addCircleLayer } from './layer/circle'
import { addLineLayer } from './layer/line'
import { addFillLayer } from './layer/fill'

export {
  initMap,
  removeLayerById,
  removeLayersByIds,
  addCircleLayer,
  addLineLayer,
  addFillLayer
}
